<h1>This works</h1>


<%= cl_image_tag @picture.photo %>
<%= form_for(@picture) do |f| %>
  <div class="field">
    <%= f.label :avatar %><br>
    <%= f.file_field :avatar %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
